import React from "react";

import s from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={s.footer}>
      <span className={s.text}> Карточки с пинтереста</span>
    </footer>
  );
};

export default Footer;
